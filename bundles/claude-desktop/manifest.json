{
  "dxt_version": "0.1",
  "name": "schift-ai-memory",
  "display_name": "Schift AI Memory",
  "version": "0.1.12",
  "description": "Local MCP bundle for connecting Claude Desktop to Schift AI Memory.",
  "author": {
    "name": "Schift",
    "email": "hello@schift.io",
    "url": "https://schift.io"
  },
  "server": {
    "type": "node",
    "mcp_config": {
      "command": "npx",
      "args": ["-y", "@schift-io/ai-memory-mcp"],
      "env": {
        "SCHIFT_DEFAULT_BUCKET": "${user_config.company_bucket}",
        "SCHIFT_COLLECTION": "${user_config.collection}"
      }
    }
  },
  "user_config": {
    "company_bucket": {
      "type": "string",
      "title": "Company memory bucket",
      "description": "Run `npx -y @schift-io/mcp login` first. The MCP server reads ~/.schift/ai-memory/config.json for credentials.",
      "default": "default",
      "required": false
    },
    "collection": {
      "type": "string",
      "title": "Daily log collection",
      "description": "The metadata collection for AI work logs.",
      "default": "__schift_ai_daily_log",
      "required": false
    }
  }
}
