{
  "dxt_version": "0.1",
  "name": "schift-ai-memory",
  "display_name": "Schift AI Memory",
  "version": "0.1.0",
  "description": "Local MCP bundle for connecting Claude Desktop to Schift AI Memory.",
  "author": {
    "name": "Schift",
    "email": "hello@schift.io",
    "url": "https://schift.io"
  },
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.js"],
      "env": {
        "SCHIFT_API_KEY": "${user_config.schift_api_key}",
        "SCHIFT_DEFAULT_BUCKET": "${user_config.company_bucket}"
      }
    }
  },
  "user_config": {
    "schift_api_key": {
      "type": "string",
      "title": "Schift API key",
      "description": "Create or copy this from Schift account settings.",
      "sensitive": true,
      "required": true
    },
    "company_bucket": {
      "type": "string",
      "title": "Company memory bucket",
      "description": "Use the default company bucket unless Schift support tells you otherwise.",
      "default": "default",
      "required": false
    }
  }
}

